name: Native employment changes release
on:
  push:
    branches: ['internal-native-employment-changes-20260923', 'master']
    paths:
      - 'api/internal-employment-changes.js'
      - 'api/internal-employment-catalog.js'
      - 'lib/internal-employment-changes.js'
      - 'lib/internal-native-employees.js'
      - 'assets/native-employment-change*'
      - 'assets/native-employee-*'
      - 'internal-dashboard.html'
      - 'scripts/migrations/104-*'
      - 'scripts/verify-native-employment-change*'
      - 'tests/native-employment-change*'
      - 'tests/fixtures/native-employee-catalog-installed-099.json'
      - 'scripts/build-friendly.mjs'
      - '.vercelignore'
      - 'vercel.json'
      - '.github/workflows/native-employment-changes-release.yml'
permissions:
  contents: read
concurrency:
  group: native-employment-changes-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: node scripts/verify-native-employment-changes-browser.mjs
      - run: node scripts/verify-native-employee-browser.mjs
      - run: node scripts/verify-native-monthly-novelties-browser.mjs
      - run: node scripts/verify-native-family-schooling-browser.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: native-employment-changes-browser-${{ github.run_id }}
          path: verification/native-employment-changes/**
          retention-days: 3
  sql:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        postgres: ['17', '18']
    services:
      postgres:
        image: postgres:${{ matrix.postgres }}
        env:
          POSTGRES_DB: native_employment_change_qa
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: native_changes_qa_only
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U postgres -d native_employment_change_qa"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:native_changes_qa_only@127.0.0.1:5432/native_employment_change_qa
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
      - run: sudo apt-get update && sudo apt-get install -y postgresql-client
      - run: psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -c 'CREATE EXTENSION IF NOT EXISTS pgcrypto; CREATE ROLE municontrol_actions_runtime_app;'
      - run: node scripts/verify-native-employment-change-sql.mjs --ci --expected-major=${{ matrix.postgres }} --require-concurrency --write-sql=verification/native-changes.sql --write-lock-sql=verification/native-changes-lock.sql
      - name: Real PostgreSQL rules and independent publication lock
        shell: bash
        run: |
          set -euo pipefail
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f verification/native-changes-lock.sql > verification/native-changes-lock.log 2>&1 &
          blocker=$!
          trap 'kill "$blocker" 2>/dev/null || true' EXIT
          ready=false
          for attempt in {1..100}; do
            if grep -q 'FIXED_NOVELTIES_QA_LOCK_READY' verification/native-changes-lock.log; then
              ready=true
              break
            fi
            if ! kill -0 "$blocker" 2>/dev/null; then
              cat verification/native-changes-lock.log
              exit 1
            fi
            sleep 0.1
          done
          test "$ready" = true
          psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f verification/native-changes.sql
          wait "$blocker"
          trap - EXIT
