name: Legal contracts release
on:
  push:
    branches: ['codex/legal-contracts-20260920', 'master']
    paths:
      - 'api/internal-legal-contracts.js'
      - 'assets/legal-contracts*'
      - 'assets/legal-matters-ui.js'
      - 'internal-legal-contracts.html'
      - 'scripts/migrations/086-legal-contracts.sql'
      - 'scripts/verify-legal-contracts-browser.mjs'
      - 'tests/legal-contracts.test.js'
      - 'scripts/build-legal-registry.mjs'
      - '.github/workflows/legal-contracts-release.yml'
permissions:
  contents: read
concurrency:
  group: legal-contracts-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci --ignore-scripts
      - run: npm run build
      - run: npx playwright install --with-deps chromium
      - run: node scripts/verify-legal-contracts-browser.mjs
      - run: node scripts/verify-legal-matters-browser.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: legal-contracts-${{ github.run_id }}
          path: verification/legal-contracts-local/**
          retention-days: 3