import {requireCompatibleInternalAccess} from '../lib/internal-access-gateway.js';import {getActionCenterSql} from './internal-actions.js';import {principalHasCapabilities} from '../lib/internal-resource-access.js';import {schoolCertificateHttp as http} from './internal-family-certificates.js';import {legalContext,legalSafeError} from '../lib/internal-legal-registry.js';import {verifyLegalAlertCenterResponse} from '../assets/legal-alert-center-model.js';
export function createLegalAlertCenterHandler(deps={}){const env=deps.env??process.env;return async(req,res)=>{http.headers(res);try{if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({ok:false,error:'Método no permitido.'});}const q=req.query||{},u=new URL(req.url||'','http://local.invalid').searchParams;if(Object.keys(q).length!==1||q.resource!=='alerts'||u.size!==1||u.get('resource')!=='alerts')return res.status(422).json({ok:false,error:'Consulta de alertas no válida.'});const caps=['legal.norm.read'],access=await(deps.authorize??requireCompatibleInternalAccess)(req,res,{env,requiredCapabilities:caps,capabilityMode:'all',allowLegacy:false,requireDataPlaneReady:false,requireCertifiedDataBinding:false});if(!access)return;if(access.mode!=='managed'||access.principal?.tenant?.source!=='membership'||!principalHasCapabilities(access.principal,caps))return res.status(403).json({ok:false,error:'No tenés permiso para consultar alertas jurídicas.'});const context=legalContext(access.principal,access.session),sql=await(deps.getSql??getActionCenterSql)(env),rows=await sql.query('SELECT public.legal_alert_center_v1($1::jsonb) AS result',[JSON.stringify(context)]);let data;try{data=verifyLegalAlertCenterResponse((Array.isArray(rows)?rows:rows?.rows)?.[0]?.result);}catch{throw Error('LEGAL_ALERT_CENTER_RESPONSE_INVALID');}return res.status(200).json({ok:true,data});}catch(e){const safe=legalSafeError(e);return res.status(safe.status).json({ok:false,error:safe.status===503?'No se pudieron verificar las alertas. Reintentá en un momento; no se modificó ningún registro.':safe.message});}};}
export default createLegalAlertCenterHandler();
