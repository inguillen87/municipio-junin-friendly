$files = Get-ChildItem -Path . -Filter *.html

foreach ($file in $files) {
    $content = Get-Content $file.FullName -Raw -Encoding UTF8
    
    $original = $content
    
    # Specific words
    $content = $content -replace 'Licitaci\?n', 'Licitación'
    $content = $content -replace 'licitaci\?n', 'licitación'
    $content = $content -replace 'T\?tulo', 'Título'
    $content = $content -replace '\?rea', 'Área'
    $content = $content -replace 'descripci\?n', 'descripción'
    $content = $content -replace 'Descripci\?n', 'Descripción'
    $content = $content -replace 'a\?o', 'año'
    $content = $content -replace 'A\?o', 'Año'
    $content = $content -replace 'p\?blico', 'público'
    $content = $content -replace 'P\?blico', 'Público'
    $content = $content -replace 'Informaci\?n', 'Información'
    $content = $content -replace 'Gesti\?n', 'Gestión'
    $content = $content -replace 'Evaluaci\?n', 'Evaluación'
    $content = $content -replace 'Jun\?n', 'Junín'
    $content = $content -replace 'Configuraci\?n', 'Configuración'
    $content = $content -replace 'M\?s', 'Más'
    $content = $content -replace 'm\?s', 'más'
    $content = $content -replace 'N\?mero', 'Número'
    $content = $content -replace 'n\?mero', 'número'
    $content = $content -replace 'D\?a', 'Día'
    $content = $content -replace 'd\?a', 'día'
    $content = $content -replace 'veh\?culo', 'vehículo'
    $content = $content -replace 'Veh\?culo', 'Vehículo'
    $content = $content -replace 'Tel\?fono', 'Teléfono'
    $content = $content -replace 'tel\?fono', 'teléfono'
    $content = $content -replace 'Atenci\?n', 'Atención'
    $content = $content -replace 'Inversi\?n', 'Inversión'
    $content = $content -replace 'Participaci\?n', 'Participación'
    $content = $content -replace 'Ejecuci\?n', 'Ejecución'
    $content = $content -replace 'Ubicaci\?n', 'Ubicación'
    $content = $content -replace 'Acci\?n', 'Acción'
    $content = $content -replace 'Direcci\?n', 'Dirección'
    $content = $content -replace 'Secci\?n', 'Sección'
    $content = $content -replace 'Asignaci\?n', 'Asignación'
    $content = $content -replace 'Pr\?ximo', 'Próximo'
    $content = $content -replace 'pr\?ximo', 'próximo'
    $content = $content -replace 'tr\?mite', 'trámite'
    $content = $content -replace 'Tr\?mite', 'Trámite'
    $content = $content -replace 'Categor\?a', 'Categoría'
    $content = $content -replace 'categor\?a', 'categoría'
    $content = $content -replace 'Pol\?tica', 'Política'
    $content = $content -replace 'Ocupaci\?n', 'Ocupación'
    $content = $content -replace 'Asociaci\?n', 'Asociación'
    $content = $content -replace 'aprobaci\?n', 'aprobación'
    $content = $content -replace 'Aprobaci\?n', 'Aprobación'
    $content = $content -replace 'p\?gina', 'página'
    $content = $content -replace 'P\?gina', 'Página'
    $content = $content -replace 'B\?squeda', 'Búsqueda'
    $content = $content -replace 'b\?squeda', 'búsqueda'
    $content = $content -replace 'Comit\?', 'Comité'
    $content = $content -replace 'comit\?', 'comité'
    $content = $content -replace 'est\?', 'está'
    $content = $content -replace 'Est\?', 'Está'
    $content = $content -replace 'aqu\?', 'aquí'
    $content = $content -replace 'Aqu\?', 'Aquí'
    $content = $content -replace 'seg\?n', 'según'
    $content = $content -replace 'Seg\?n', 'Según'
    $content = $content -replace 'Tambi\?n', 'También'
    $content = $content -replace 'tambi\?n', 'también'
    $content = $content -replace 'qu\?', 'qué'
    $content = $content -replace 'Qu\?', 'Qué'
    $content = $content -replace 'c\?mo', 'cómo'
    $content = $content -replace 'C\?mo', 'Cómo'
    $content = $content -replace 'cu\?ndo', 'cuándo'
    $content = $content -replace 'Cu\?ndo', 'Cuándo'
    $content = $content -replace 'd\?nde', 'dónde'
    $content = $content -replace 'D\?nde', 'Dónde'
    $content = $content -replace 'qui\?n', 'quién'
    $content = $content -replace 'Qui\?n', 'Quién'
    $content = $content -replace 'cu\?l', 'cuál'
    $content = $content -replace 'Cu\?l', 'Cuál'
    $content = $content -replace 'cu\?ntos', 'cuántos'
    $content = $content -replace 'Cu\?ntos', 'Cuántos'
    $content = $content -replace 'cu\?nto', 'cuánto'
    $content = $content -replace 'Cu\?nto', 'Cuánto'
    $content = $content -replace 'podr\?a', 'podría'
    $content = $content -replace 'Podr\?a', 'Podría'
    $content = $content -replace 'ser\?', 'será'
    $content = $content -replace 'Ser\?', 'Será'
    $content = $content -replace 'har\?', 'hará'
    $content = $content -replace 'Har\?', 'Hará'
    $content = $content -replace 'habr\?', 'habrá'
    $content = $content -replace 'Habr\?', 'Habrá'
    $content = $content -replace 'tendr\?', 'tendrá'
    $content = $content -replace 'Tendr\?', 'Tendrá'
    $content = $content -replace 'est\?n', 'están'
    $content = $content -replace 'Est\?n', 'Están'
    $content = $content -replace 'ser\?n', 'serán'
    $content = $content -replace 'Ser\?n', 'Serán'
    $content = $content -replace 'har\?n', 'harán'
    $content = $content -replace 'Har\?n', 'Harán'
    $content = $content -replace 'habr\?n', 'habrán'
    $content = $content -replace 'Habr\?n', 'Habrán'
    $content = $content -replace 'tendr\?n', 'tendrán'
    $content = $content -replace 'Tendr\?n', 'Tendrán'
    $content = $content -replace 'podr\?n', 'podrán'
    $content = $content -replace 'Podr\?n', 'Podrán'
    $content = $content -replace 'deber\?', 'deberá'
    $content = $content -replace 'Deber\?', 'Deberá'
    $content = $content -replace 'deber\?n', 'deberán'
    $content = $content -replace 'Deber\?n', 'Deberán'
    $content = $content -replace 'tambi\?n', 'también'
    $content = $content -replace 'Tambi\?n', 'También'
    $content = $content -replace 'adem\?s', 'además'
    $content = $content -replace 'Adem\?s', 'Además'
    $content = $content -replace 'despu\?s', 'después'
    $content = $content -replace 'Despu\?s', 'Después'
    $content = $content -replace 'atr\?s', 'atrás'
    $content = $content -replace 'Atr\?s', 'Atrás'
    $content = $content -replace 'm\?s', 'más'
    $content = $content -replace 'M\?s', 'Más'
    $content = $content -replace 'a\?n', 'aún'
    $content = $content -replace 'A\?n', 'Aún'
    $content = $content -replace 's\?lo', 'sólo'
    $content = $content -replace 'S\?lo', 'Sólo'
    $content = $content -replace 'S\?', 'Sí'
    $content = $content -replace 's\?', 'sí'
    $content = $content -replace 'M\?', 'Mí'
    $content = $content -replace 'm\?', 'mí'
    $content = $content -replace 'T\?', 'Tú'
    $content = $content -replace 't\?', 'tú'
    $content = $content -replace 'El\?', 'Él'
    $content = $content -replace 'el\?', 'él'
    $content = $content -replace 'c\?digo', 'código'
    $content = $content -replace 'C\?digo', 'Código'
    $content = $content -replace 'art\?culo', 'artículo'
    $content = $content -replace 'Art\?culo', 'Artículo'
    $content = $content -replace 'pr\?ctica', 'práctica'
    $content = $content -replace 'Pr\?ctica', 'Práctica'
    $content = $content -replace 't\?rmino', 'término'
    $content = $content -replace 'T\?rmino', 'Término'
    $content = $content -replace '\?ltimo', 'último'
    $content = $content -replace '\?ltimo', 'Último'
    $content = $content -replace '\?nico', 'único'
    $content = $content -replace '\?nico', 'Único'
    $content = $content -replace 'r\?pido', 'rápido'
    $content = $content -replace 'R\?pido', 'Rápido'
    $content = $content -replace 'f\?cil', 'fácil'
    $content = $content -replace 'F\?cil', 'Fácil'
    $content = $content -replace 'dif\?cil', 'difícil'
    $content = $content -replace 'Dif\?cil', 'Difícil'
    $content = $content -replace '\?til', 'útil'
    $content = $content -replace '\?til', 'Útil'
    $content = $content -replace 'D\?lar', 'Dólar'
    $content = $content -replace 'd\?lar', 'dólar'
    $content = $content -replace 'c\?lculo', 'cálculo'
    $content = $content -replace 'C\?lculo', 'Cálculo'
    $content = $content -replace 'an\?lisis', 'análisis'
    $content = $content -replace 'An\?lisis', 'Análisis'
    $content = $content -replace 'estad\?stica', 'estadística'
    $content = $content -replace 'Estad\?stica', 'Estadística'
    $content = $content -replace 'pol\?tica', 'política'
    $content = $content -replace 'Pol\?tica', 'Política'
    $content = $content -replace 'econ\?mico', 'económico'
    $content = $content -replace 'Econ\?mico', 'Económico'
    $content = $content -replace 'p\?liza', 'póliza'
    $content = $content -replace 'P\?liza', 'Póliza'
    $content = $content -replace 'tr\?nsito', 'tránsito'
    $content = $content -replace 'Tr\?nsito', 'Tránsito'
    $content = $content -replace 'v\?a', 'vía'
    $content = $content -replace 'V\?a', 'Vía'
    $content = $content -replace 'R\?o', 'Río'
    $content = $content -replace 'r\?o', 'río'
    $content = $content -replace 'Comisi\?n', 'Comisión'
    $content = $content -replace 'comisi\?n', 'comisión'
    $content = $content -replace 'versi\?n', 'versión'
    $content = $content -replace 'Versi\?n', 'Versión'
    $content = $content -replace 'opci\?n', 'opción'
    $content = $content -replace 'Opci\?n', 'Opción'
    $content = $content -replace 'situaci\?n', 'situación'
    $content = $content -replace 'Situaci\?n', 'Situación'
    $content = $content -replace 'condici\?n', 'condición'
    $content = $content -replace 'Condici\?n', 'Condición'
    $content = $content -replace 'soluci\?n', 'solución'
    $content = $content -replace 'Soluci\?n', 'Solución'
    $content = $content -replace 'petici\?n', 'petición'
    $content = $content -replace 'Petici\?n', 'Petición'
    $content = $content -replace 'decisi\?n', 'decisión'
    $content = $content -replace 'Decisi\?n', 'Decisión'
    $content = $content -replace 'posici\?n', 'posición'
    $content = $content -replace 'Posici\?n', 'Posición'
    $content = $content -replace 'funci\?n', 'función'
    $content = $content -replace 'Funci\?n', 'Función'
    $content = $content -replace 'reuni\?n', 'reunión'
    $content = $content -replace 'Reuni\?n', 'Reunión'
    $content = $content -replace 'opini\?n', 'opinión'
    $content = $content -replace 'Opini\?n', 'Opinión'
    $content = $content -replace 'relaci\?n', 'relación'
    $content = $content -replace 'Relaci\?n', 'Relación'
    $content = $content -replace 'cuesti\?n', 'cuestión'
    $content = $content -replace 'Cuesti\?n', 'Cuestión'
    $content = $content -replace 'creaci\?n', 'creación'
    $content = $content -replace 'Creaci\?n', 'Creación'
    $content = $content -replace 'formaci\?n', 'formación'
    $content = $content -replace 'Formaci\?n', 'Formación'
    $content = $content -replace 'presentaci\?n', 'presentación'
    $content = $content -replace 'Presentaci\?n', 'Presentación'
    $content = $content -replace 'representaci\?n', 'representación'
    $content = $content -replace 'Representaci\?n', 'Representación'
    $content = $content -replace 'operaci\?n', 'operación'
    $content = $content -replace 'Operaci\?n', 'Operación'
    $content = $content -replace 'organizaci\?n', 'organización'
    $content = $content -replace 'Organizaci\?n', 'Organización'
    $content = $content -replace 'administraci\?n', 'administración'
    $content = $content -replace 'Administraci\?n', 'Administración'
    $content = $content -replace 'comunicaci\?n', 'comunicación'
    $content = $content -replace 'Comunicaci\?n', 'Comunicación'
    $content = $content -replace 'aplicaci\?n', 'aplicación'
    $content = $content -replace 'Aplicaci\?n', 'Aplicación'
    $content = $content -replace 'publicaci\?n', 'publicación'
    $content = $content -replace 'Publicaci\?n', 'Publicación'
    $content = $content -replace 'clasificaci\?n', 'clasificación'
    $content = $content -replace 'Clasificaci\?n', 'Clasificación'
    $content = $content -replace 'certificaci\?n', 'certificación'
    $content = $content -replace 'Certificaci\?n', 'Certificación'
    $content = $content -replace 'planificaci\?n', 'planificación'
    $content = $content -replace 'Planificaci\?n', 'Planificación'
    $content = $content -replace 'modificaci\?n', 'modificación'
    $content = $content -replace 'Modificaci\?n', 'Modificación'
    $content = $content -replace 'evaluaci\?n', 'evaluación'
    $content = $content -replace 'Evaluaci\?n', 'Evaluación'
    
    # Emojis manually specified in context
    $content = $content -replace '\?\? Nueva Licitación', '📋 Nueva Licitación'
    $content = $content -replace '\?\? Exportar', '📥 Exportar'
    $content = $content -replace '\?\? Flujo', '🔄 Flujo'
    $content = $content -replace '\?\? Registro', '📋 Registro'
    $content = $content -replace '\?\? Activas', '✅ Activas'
    $content = $content -replace '\?\? Proveedores', '🏢 Proveedores'
    $content = $content -replace '\?\? Calendario', '📅 Calendario'
    $content = $content -replace '\?\? AI Pliegos', '🤖 AI Pliegos'
    $content = $content -replace '\?\? AI Auditor', '🔍 AI Auditor'
    $content = $content -replace '\?\? IA', '🤖 IA'
    $content = $content -replace '\?\? Guardar', '💾 Guardar'
    $content = $content -replace '\?\? Crear', '✅ Crear'
    $content = $content -replace '\?\? Nuevo', '➕ Nuevo'
    $content = $content -replace '\?\? Buscar', '🔍 Buscar'
    $content = $content -replace '\?\? Filtrar', '🎛️ Filtrar'
    $content = $content -replace '\?\? Cancelar', '❌ Cancelar'
    $content = $content -replace '\?\? Editar', '✏️ Editar'
    $content = $content -replace '\?\? Eliminar', '🗑️ Eliminar'
    $content = $content -replace '\?\? Detalles', '📄 Detalles'
    $content = $content -replace '\?\? Descargar', '⬇️ Descargar'
    $content = $content -replace '\?\? Subir', '⬆️ Subir'
    $content = $content -replace '\?\? Analizar', '📊 Analizar'
    
    # Generic ?? replacement based on words following it
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(Guardar|Crear|Confirmar|Aprobar)', '✅ $1')
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(Cancelar|Eliminar|Rechazar)', '❌ $1')
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(Editar)', '✏️ $1')
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(Buscar)', '🔍 $1')
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(Nuevo|Más|Agregar)', '➕ $1')
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(Exportar|Descargar)', '📥 $1')
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(Importar|Subir)', '📤 $1')
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(Imprimir)', '🖨️ $1')
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(Actualizar|Cargar|Flujo)', '🔄 $1')
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(AI|IA)', '🤖 $1')
    $content = [System.Text.RegularExpressions.Regex]::Replace($content, '\?\?\s*(Licitación|Registro)', '📋 $1')
    
    # Any leftover ?? mapping to 🔹
    $content = $content -replace '\?\?', '🔹'
    
    # Title specific replacements (like Licitaciones y Compras ? MuniControl)
    $content = $content -replace 'Licitaciones y Compras \? MuniControl', 'Licitaciones y Compras | MuniControl'
    $content = $content -replace 'Municipal Jun\?n', 'Municipal Junín'

    if (-not ($content -match '<meta charset="UTF-8"')) {
        $content = $content -replace '<head>', "<head>`n  <meta charset=`"UTF-8`" />"
    }

    if ($content -ne $original) {
        Set-Content -Path $file.FullName -Value $content -Encoding UTF8
        Write-Host "Fixed $($file.Name)"
    }
}
